import { TranslateKey } from '../../utils';

export enum ETabs {
  SELECT_FILES,
  TABLE,
  ANALYTICS,
}

export const TAB_NAME_KEYS: TranslateKey[] = [
  'tabs.selectFiles',
  'tabs.table',
  'tabs.analytics',
  'tabs.help'
];
